import buildImage from 'darkwoods/utils/build-image';
import { module, test } from 'qunit';

module('Unit | Utility | build image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = buildImage();
  assert.ok(result);
});
