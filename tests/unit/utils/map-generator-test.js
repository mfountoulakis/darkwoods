import mapGenerator from 'darkwoods/utils/map-generator';
import { module, test } from 'qunit';

module('Unit | Utility | map generator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mapGenerator();
  assert.ok(result);
});
